<h2>Create ToDo list</h2>
<%= simple_form_for :task_list, {:url => task_list_index_path()} do |f| %>
  <%= f.fields_for @todo_list do |ff|%>
    <%= ff.input :name %>
    <%= ff.input :is_private %>
    <%= ff.submit "Create" %>
  <% end %>
<% end %>
