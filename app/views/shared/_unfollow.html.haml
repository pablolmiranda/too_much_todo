.todo_list{ :class => "#{todo_list.is_private? ? "private":"public"}", :id => "todo_list-#{todo_list.id}"}
 = todo_list.name
 %span= link_to 'Unfollow', unfollow_task_list_path(todo_list), :id => "list-#{todo_list.id}", :remote => true, :confirm => "Are you sure?" 

